The first file named array.cpp contains basic operations on array i.e. Insertion, Deletion, Updation, Searching.
The second file named ndto1darray.cpp contains the logic to get address of and element from an nd array when mapped into 
1d array i.e. showing how it is stored in the contiguous memory locations.
